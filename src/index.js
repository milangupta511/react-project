import React from 'react';
import ReactDOM from 'react-dom';
import DesignerNewsContainer from './DesignerNewsContainer';

ReactDOM.render(
  <DesignerNewsContainer />,
  document.getElementById('root')
);
